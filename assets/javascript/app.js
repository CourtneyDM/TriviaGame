$(document).ready(function () {
    var questions = [];

    // TODO: Use an array to store 10 questions, and for each questions have 4 answers. Each question will have a number identifier, and each answer will have a number identifier.
});